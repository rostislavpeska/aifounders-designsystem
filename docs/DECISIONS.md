# Diverged tokens & operator decisions

**This file is the authoritative decision log.** Every ruling below is
operator-ratified and canonical until superseded by a newer, dated entry.

Moved out of the styleguide (Decisions tab retired by operator verdict,
2026-07-03) — divergences are data on the Colors/Typography tabs, laws live
in the gate + skills. Per-brand values preserved from harvest + laws decided
during Batch 0–1. Full history:
`docs/proposals/AGENTIC-DS-RESEARCH.md` + the build manifest.

| Token / subject | Ruling |
|---|---|
| `--heading-md-font / -weight` | AIG: Lazzer/900 · AIF: Inter/800 |
| `--heading-sm-leading` | AIG: 1.35 · AIF: 1.2 |
| `--lead-leading` | AIG: 1.5 · AIF: 1.6 (rendered reality; token said 1.7 = fiction) |
| `--description-weight` | AIG: 400 · AIF: 500 |
| `--caption-weight` | AIG: 400 · AIF: 500 |
| `--container-wider` | AIG: 1100px · AIF: 980px |
| `--status-warning` | UNIFIED 2026-07-03: #a16207 both brands (operator: #b45309 read as red; the yellowish AIG value wins — divergence removed, GM exception on AIF) |
| `--color-secondary-*` (aiguild) | INTENTIONAL CHANGE vs live site: exact twin of aifounders primary per mirror law (was off-copy). GM exception (`.course-accent--secondary`). |
| font-weight 300 (Light) | BANNED. Was fiction or strays; becomes Regular at migration; drop 300 from font loading at adoption. |
| blockquote | ONE VOICE = intro perex (verified live; AIF quiet 18px base was dead code — killed, GM exception). |
| newsletter capture | ONE component, ZERO newsletter-specific button styles: AIF = plain primary on BRAND surface (auto-dark), AIG = plain tertiary on DARK surface. GM exceptions: footer border 1px→2px; AIG footer button subtle-filled → tertiary-on-dark. `.btn--newsletter-*` deleted (never shipped). |
| button-group / outline / primary-inverted | button-group = pure layout primitive (pill look = publisher-plugin switch pills); `--outline` deprecated (dup of secondary); `--primary-inverted` reclassified → `.btn--newsletter-primary`. |
| chatbot bubble | NOT a DS component (operator 2026-07-03): production bubble lives in the themes (aigb-chat mu-plugin) and stays there; `.btn-floating` stripped. |

## Later verdicts

| Date | Subject | Ruling |
|---|---|---|
| 2026-07-03 | `subtitle` style | RETIRED — SG 60, zero live usage in the 7-page harvest. |
| 2026-07-03 | Decisions tab | RETIRED — content lives here; the styleguide stays generated data. |
| 2026-07-03 | Accent overlay idiom | Badge hover overlays = `color-mix(in srgb, var(--accent) 15%, transparent)` — no alpha entries in the palette; overlay follows its accent. The location badge's harvested #B148C6 matched NO palette value (stale pre-rebrand purple) → corrected to `magenta`. GM exception. |
| 2026-07-03 | `.text--body` list leading | Harvested 1.55 NORMALIZED to `leading-body` (1.5) — no orphan leading values. GM exception. |
| 2026-07-03 | `spacing-6` | ADDED to the primitive scale (0.375rem) — small-button icon gaps and smart-button internals were raw 6px; the 4→8 jump was a real gap. |
| 2026-07-03 | Numbered headings (`.numbered-headings`) | NEW text element, harvested from both themes' `.course-syllabus` (live on course-detail). ONE mechanism (auto-numbered flex heading + content indent); number rendered as a brand-colored **tile** (`--brand` fill → yellow AIG / blue AIF, `--text-on-brand` digit). **UNIFIED to AIG's tile on both brands** — AIF live renders a plain "N." (GM exception, ⚠️ operator veto point: keep tile-on-both or revert AIF to plain numbers?). Class alias `course-syllabus → numbered-headings` (adoption; NOT in rename-map.json — that's tokens only). Heading text = page-context heading-md. Tile geometry 44/32px = component calibration. |
| 2026-07-04 | Icon stroke — STEPPED (supersedes constant-1.5, which superseded the ratio-based stepping) | Outline icons: **<16px → `--stroke-1`** (fine) · **16–32px → `--stroke-1_5`** (default) · **>32px → `--stroke-3`** (heavy), all screen-px via `non-scaling-stroke` (no viewBox math). Boundary RULED by operator: **16px itself is 1.5 — only 14 (below 16) is fine**; 32 itself is default. `aigds_icon()` picks the step from `size`; `.icon--sm` (14px) pairs with the fine step. check-bold stays fixed `--stroke-3`; shape/colored exempt. Gate-asserted on the icon-system ramp. |
| 2026-07-04 | Accordion | Harvested 1:1 (AIF /newsletter FAQ + AIG course detail). Unifications: AIG 1px card border on both brands; AIG synced icon transition; Figma max-width 844 dropped. Two modes: independent (default) / exclusive via `[data-accordion="exclusive"]`. Title = heading-xs bundle, body = body-lg, icon --deep. 0 new tokens. |
| 2026-07-04 | Data-table mobile | `.table-scroll` horizontal-scroll wrapper harvested from AIF production = the DEFAULT for wide tables; card-collapse NOT adopted (that intent = record list); a mobile-transform special override for landing pages is TRACKED, opt-in only. |
| 2026-07-04 | Distill batch 1 (breadcrumb · pagination · nav-tabs) | All three shipped, 0 new tokens. GM exceptions (precedent-covered, veto points): breadcrumb + nav-tabs links ALIGNED TO THE LINK IDIOM (live hover removed the underline — outlawed); pagination SQUARE (live radius-sm, radius-retirement ruling) + `spacing-64→56` snap + declared medium/semibold weights were FICTION (v1 vars undefined, live renders 400 — rendered reality wins) + hover scoped to `<a>` (live recolored the static current chip); nav-tabs renamed generic from `.author-tabs` (alias at adoption), active chip = palette-direct `--white` (var(--bg) vanishes on the brand hero), live 1.7 leading → `--lead-leading`; the centering JS ported with an offsetLeft port-fix. Exclusion chain extended: `.breadcrumb__link`, `.nav-tabs__tab`, `.page-numbers`. |
| 2026-07-04 | Distill batch 2 (reference card · persona card) — **SURFACES REPLACE VARIANTS** | Operator ruling: ONE canonical card, appearance from the BACKGROUND. Reference card: production's `reference-card section-dark` markup IS the model — the `--case-study` override block + the AIF newsletter `.testimonial-card` skin die at adoption; gaps unified to 40; fill = `--raised` (role over one-step-off live values); quote voice Light/300 fiction → regular; `#8a6a00` link hardcode → global chain; quote mark = `--bullet`; HARVESTED `quote-brackets` art registered into the icon catalog (shape). Persona card: **`.persona-card` REMOVED from the dark-3 scope map** (build.mjs) — surface-riding via roles; `--light` variant collapsed; Figma max-width 262 dropped; voices = heading-sm/description/body-md/meta bundles (first DS consumers of the reserved description+meta bundles). Testimonials-carousel = separate composition, tracked as candidate. |
| 2026-07-05 | `card-heading` type style — NEW bundle (operator-ordered) | The listing-card headline stays **Inter on BOTH brands** at heading-md size — Lazzer renders terribly at 28px in cards (heading-md maps to Lazzer/900 on AIG). Bundle: size-28 / font-primary / weight-extrabold / leading-heading, CONSTANT per MECHANISM LAW (card/UI element, no mobile step). Public class `.card-heading`; consumer: the preview-card default headline. Gate asserts Inter + never-Lazzer on both brands. |
| 2026-07-05 | Preview card — the operator's size × slots model | Production's SIX listing-card variants (.article --archive/--editorial/--compact/--events/--signal/--position + has/no-image) COLLAPSED into `.preview-card`: **size** (default heading-md · `--condensed` heading-sm — exactly production's text-h4 override set) × **slots** (photo/headline/meta/badges/skills/text/actions; headline required). No-photo top hairline is automatic (`:has()`). Linked photo = harvested 1.02 hover zoom; headline links = card-title-link idiom (per-variant !important link rules die). Meta = caption voice between hairlines, separators are content. Skills (positions) = DS `skills` icon + linked names. Text: ~30 words articles / ~20 events / SIGNALS full paragraphs (ruled exception). Actions = 1..n bottom-pinned link-buttons, ICON GRAMMAR: arrow-right internal · source external · edit edit · destructive delete (the My-Articles management row: edit+view+delete). GM: max-widths dropped, skills 1.9/20px snapped, JS-template drift (h4/outline-btn) unified to canon at adoption. 0 new tokens. |
| 2026-07-07 | PUBLIC-REPO §6 — all four verdicts RESOLVED; cleanup spec'd, execution deferred | Operator (last Fable night): (1) **FULL AIFDS RENAME** — `aigds_`→`aifds_`, `AIGDS_`→`AIFDS_`, handles `aigds-`→`aifds-`, `aig-design-system.php`→`aifounders-designsystem.php`, text-domain + slug `aifounders-designsystem`, display "AI Founders Design System" (~512 occurrences / 64 files, census in the spec; DECISIONS.md history stays untouched). (2) LICENSE holder → **AI Founders**. (3) `docs/proposals/` ships **all-in** publicly. (4) Version → **2.0.0-rc.1** (2.0.0 final at theme adoption). Execution = [REPO-CLEANUP-SPEC](proposals/REPO-CLEANUP-SPEC.md) (phases R0–R8, gate checkpoints, operator handoffs, rollback, executor kickoff prompt) — DEFERRED until the overnight Figma import agent lands, because the rename changes the `aigds_styleguide` param + PHP prefixes the import agent live-validates against; running both in one tree would sabotage the import. |
| 2026-07-07 | Consent note = TERTIARY text (operator, two vetoes in one day: the palette-direct read AND the harvested color itself) | First veto: the note's `var(--tint-light)` read was a 3-layer VIOLATION ("I doubt the token has been defined for subtle disclaimer notes") — I minted `--text-note` (base black, dark scopes → tint-light) preserving the harvested cream. Second veto, same day: **the COLOR was wrong too** — "I wanted basically something like tertiary text — rather grayish than pure white and definitely not a brand color." FINAL: the note reads **`--text-tertiary`** everywhere (gray-600 on light, dark-300 on the dark band; NEW brand-scope mapping `text-tertiary → black` — gray on saturated brand was a REAL axe fail the gate caught, and black chrome is the brand scope's standing philosophy + production's hero-note behavior); `--text-note` KILLED same day (roles back to 51); the harvested brand-tint cream (#fffce3/#c1d1d9, production's `--color-primary-neutral-light`) DIES — a deliberate delta from production. The `--footer-dark` color override stays dead (scope carries the flip); links keep the `--link` pop. Gate pins the dark-band gray per brand. THE LINE stands: palette-direct only for per-case-ruled ACCENT reads; a reusable VOICE is role territory — and when a harvested value gets flagged, ask whether the OPERATOR wants the value at all, not just where it should live architecturally. |
| 2026-07-07 | FIGMA IMPORT — architecture RESOLVED (4 verdicts, all as recommended) | [FIGMA-IMPORT](proposals/FIGMA-IMPORT.md) research (bundled figma-use + figma-generate-library skills mined, web sweep, manifest delta analysis) → operator ratified: (1) **Semantic collection modes = ALL 8 CSS scopes** (light-1/2/3, dark-1/2/3, brand, support — the exact CSS-class mirror; fits since Figma's Oct-2025 raise: Professional = 10 modes/collection); brand axis lives ONLY in Palette (AIF·AIG modes), breakpoint ONLY in Type Primitives — one axis per collection, never duplicated (Figma can't link same-named modes across collections). (2) **Type clamps → Desktop·Mobile modes** on Type Primitives (96/80/64 @1440 vs 48/40/36 @390 — the shots.mjs viewports). (3) **Staged tiers**: session 1 = variables + text styles + Foundations + 117 icons + Tier-0 atoms → operator reviews → molecules/organisms follow. (4) **`ds-figma-import` skill ratified** (`.claude/skills/`, thin — our mapping + protocol; Plugin-API mechanics delegated to the bundled pair). Facts of record: semantic aliasing is mechanical (manifest `ref` = palette name, zero hex-matching); scope deltas — dark-2 differs from dark-1 by ONE role (`bg`); **Code Connect is Org/Enterprise-only** → `figma_node_id` backfills from committed `assets/figma-map.json`, `code_connect` stays null ("deferred: figma plan"); `codeSyntax WEB = var(--exact-css-name)` carries token fidelity regardless of plan. |
| 2026-07-06 | Divided-cell padding 32 → 24 (operator: the outer edge "a bit too much") | The edge read 56px (24 crust + 32 cell). Snapped the divided cell padding to `--spacing-24` = production dark-blurb's OWN cell padding (harvest `components.css:3676`); edge 24+24=48 now ≈ inter-cell 24+1+24=49 — optically uniform around every separator. |
| 2026-07-07 | Consent note = STANDARD capture anatomy (operator: "the legal disclaimer should be a standard part of both email inputs — cleaner") | Production verified on BOTH sites: `.mc4wp-consent-note` ships with every capture (AIF hero: plain `--text` voice on brand, leading 1.7; AIG band: pale tint #fffce3, margin 2). Canon: ONE base rule (`--body-md-size` / `--leading-body` / `--text`, links inherit+underline; GM: margin→8, leading→body) + the footer-dark skin recolors to **`var(--tint-light)`** — the harvested values are the palette tint-light pair EXACTLY on both brands (the reserved tint ramp's first consumer; GM palette-direct, quieter than --text by design) with `--link` links. Slot optional to render, never to restyle; all three specimen captures carry it; gate asserts voice + link. |
| 2026-07-07 | THE CONJOIN LAW — capture geometry rationalized into the input-pair (operator: stacked band lost its right border; "solve universally once forever") | The join border-collapse and stretch-to-match are ROW-ONLY facts, expressed ONCE: the `.input-pair` primitive block now also drives `.aif-ecomail-form .mc4wp-form-fields` (hero + footer-dark become CONSUMERS carrying skin only — colors, heights, padding). Stacked state = all four borders, own heights, 8px breath (GM: production stacked gap 0 → 8; with borders restored, 0 reads as a double-border seam). One breakpoint 600 (GM: footer-dark's harvested 767 → 600). The unscoped conjoined-geometry block (whose `height:auto` collapsed the stacked submit) DIED. Gate asserts the stacked border-right + gap so the join can never leak into the stack again. |
| 2026-07-07 | Newsletter capture — the FLEX-BASIS TRAP (operator catch: mobile field lost its height) | `flex: 1` on the capture wrappers meant basis 0, and in the mobile COLUMN stack flex-basis OVERRIDES the height property — the field (and the footer-dark submit, which relied on row-stretch) collapsed. Production verified at 390 first (AIG band: field+submit 52/52; AIF hero: 60+borders/60): wrappers → `flex: 1 1 auto` (identical in row, respects height in column); footer-dark submit gains its production `min-height: 52px` (the hero context always had its 60). Gate asserts the stacked 52/52 so it can never silently collapse again. |
| 2026-07-06 | Footer social icons — QUATERNARY rest (operator, post-ship; supersedes the text-secondary try same day) | Operator: text-secondary read "almost white" — BROWSER-VERIFIED live production renders the white-fill SVG imgs at ~rgb(106,106,106) (a dimmer the CSS walker could not find — rendered reality wins). Rest = `--dark-400` palette-direct (the footer's quaternary voice, same GM as the eyebrows; #69707a AIF is near-exact to the sampled value), waking to `--text` on hover. The harvested opacity-.7 fade stays dead (backwards on a muted currentColor icon). |
| 2026-07-06 | FOOTER — the closing chrome shipped (4 operator rulings) | ONE footer, band stack, all optional except legal ([footer.md](components/footer.md)). **RULINGS**: dark-2 LIVES (the newsletter band's scope class IS its surface — bg dark-900, exact); the dark-1 `.footer` alias DROPPED from the scope map (live markup on both sites already emits `.footer.section-dark` — persona precedent); capture = an OPTIONAL footer slot (AIG on; AIF's blue `.newsletter-cta` stays page content — its own future row); footer eyebrows stay QUATERNARY (palette-direct `--dark-400` GM, highlight-chip precedent — exact both brands). Columns = THE CANON (`.stack-grid` + `.blurb`; footer-blurb dies). Roles all exact: subtle-link = link/link-hover, legal = text-tertiary/secondary, divider = raised (dark-850 verbatim). **The band's negative pull is knob-synced** (`calc(-1*--footer-pad-top)` — production hardcoded −120/−48/−40). `--chatbot-clear` axis = AIG's harvested clearance (56/80 bottoms). GM (veto points): legal-link hover KEEPS the underline (link law; production removed it); social gap 20→16; partners opacity-stack + `zoom:.5` die → one 0.5 + 44px heights; leading snaps (1.28→heading, 1.6→body); social icons pinned `--text` (DS icons ride currentColor; production imgs). Link-exclusion chains (main + section-dark) extended with all four footer idioms — the harvested `!important` pair dies. 0 new tokens. |
| 2026-07-06 | Header scrolled surface + progress axis (operator, post-ship) | The shrunk LIGHT chrome = **WHITE + shadow** → `--bg-base` (pop-to-page-surface role; supersedes the raised GM — production's near-white bg-secondary reads as white intent); dark keeps `--raised` (#1c1c1c harvested) + shadow. The dropdown panel follows the same pair. **The progress bar is an AXIS, not part of the scrolled state**: only article pages render `.reading-progress`; every other page shrinks bare (specimen shows both). |
| 2026-07-06 | Header mobile polish (operator, post-ship) | Mobile items get the DESKTOP weight (`--weight-medium`) and PRIMARY text color only (the `--sub` secondary tint dies); the harvested parent chevron (`.mobile-nav-arrow` on has-sub items) DELETED — with flat always-expanded nesting it signaled an interaction that doesn't exist. `--has-sub` leaves the anatomy (nothing left to mark); the `--sub` arrow prefix alone marks the level. |
| 2026-07-06 | HEADER — distilled from byte-identical twins (anatomy-based, operator-ordered; 3 AskUserQuestion rulings) | ONE chrome component, slots not content ([header.md](components/header.md)). **RULINGS**: `--nav-active` = NEW semantic role over NEW palette `brand-muted` (#98C3D9 AIF / #63531B AIG — the harvested dimmed-brand actives, 7 hardcode sites die); `--progress-fill` = NEW role over NEW palette `progress` (per-brand harvested reality: AIF lime #a0c92f / AIG magenta #d72da4 — the rank inconsistency dissolves into the role); mobile item 18→**16** both (AIF twin wins — one voice with desktop). **SURFACES REPLACE VARIANTS**: `--light/--dark` DIE → `.surface-support`/`.section-dark`; rest bg `--bg`, scrolled `--raised` (GM: AIF #f7f8fb→raised; AIG #1c1c1c exact). **THE HEADER RUNG**: `.main-header .btn` = real DS button at 36px (bar precedent) — `.nav-item--cta` dies. **A11Y GM** (mapped production gaps, fixed): dropdowns open on `:focus-within` + button triggers get aria; `.nav-dropdown-item--active` got its rule (was inert); scroll-lock canon (AIG's was declared-but-inert); burger ships `aria-expanded`. GM snaps: scrolled glow `0 0 60px rgba(7,7,8,.2/.3)` → `--shadow-xl`; dropdown pad 16/20 → 16; progress track → `--border`; burger path = AIG's `M3 5h18`. Modes: fixed+shrink (50px CALIBRATED) / `--overlay` (hero pages, engine bails); NEVER hide-on-scroll. Dropdown ceiling = 2 levels (harvested; flyouts = future verdict if ever needed). Dead code not ported. ⚠️ veto points: shadow-xl vs the harvested glow; track --border vs AIG's bright #e5e5e5; AIG's 3 light-header templates flagged accidental. |
| 2026-07-06 | Divided grid — SINGLE-OWNERSHIP PADDING (operator: doubled edge padding "reads cheap") | The crust (or host container) owns the edge inset; cells pad INTERIOR sides only — every boundary, edge or separator, gets exactly ONE `--spacing-24` breath per side, and the separators stop at the content extent. Supersedes the "edge 48 ≈ inter-cell 49" rationale of the 32→24 snap (the 24 value stands). Column math = the default 3 (nth pair overridable, preview-grid precedent); full rows assumed. |
| 2026-07-06 | THE PIZZA PATTERN EVERYWHERE — separators are INTERIOR-ONLY (operator veto: "I want the pizza pattern everywhere... I still see the broken top border") | The whole benefits family uses ONE separator grammar: the `--divided` stack grid — lines BETWEEN objects only, never above the first row, never on any outer edge (an edge line is crust territory and reads as BROKEN). Consequences: **`--rule-top` DELETED** (the blurb has no rule axes at all — the harvested cert per-cell rule-tops DIE at adoption), **`--rules` mode DELETED** (built earlier the same day on a misread — it preserved and extended the top border instead of killing it), the exclusion law is moot (nothing left to exclude), the lp-what grid's lone 2px top border DIES. Benefits rows + the newsletter landing = `stack-grid--divided`; fill surface-rides (`--stack-fill` defaults to `--bg`). Rule-LEFT stays blockquote-family territory. |
| 2026-07-06 | ~~STACK GRID `--rules`~~ — **VETOED same day, removed** (misread of the order below) | The benefits' opening rule and the lp-what "Šest oblastí" grid border are the SAME element: a **continuous** row rule above EVERY row, ending exactly ON the edge objects' edges. New grid mode `.stack-grid--rules` (horizontal-only pizza law): `column-gap: 0`, columns breathe via cell `padding-inline` 16 (= the old 32 gap), outer cells drop outer padding so the line stops on the content edge. ALIGNMENTS (GM, veto points): production cert rows rendered per-cell rule SEGMENTS with holes at the gaps → continuous; lp-what's row 2 was BARE (only the grid top had the 2px border) → every row ruled. Cells inside `--rules` carry no `--rule-top` (the grid supplies it); `--rule-top` remains for standalone blurbs. Column math = the default 3 (all production instances); other counts override the nth pair (preview-grid precedent). |
| 2026-07-06 | BENEFITS FAMILY — RESOLVED: blurb + stack grid, NO container component (operator architecture) | The operator's model, challenged + refined, shipped: **`.blurb`** (slots on/off: rule-top · icon · eyebrow · headline · text · action) + **`.stack-grid`** (universal N-up; `--numbered` — the counter is a ROW duty; `--divided` — **THE PIZZA LAW**: separators belong to the grid, span only its content extent, never the crust; mechanism = grid paints `--border`, 1px gaps show the line, cells paint the `--stack-fill` knob). **NO container component** — the box = a consumer recipe (raised fill + crust padding + the −80px bleed, harvested geometry). **CLOSED SETS**: 3 headline rungs (heading-xs 18 · heading-sm 22 · **NEW bundle `benefit-display`**, approved: Inter fluid clamp 24→32 both brands, leading 1.15→`--leading-tight` GM, tracking −0.01→`--tracking-display` GM) + 3 text sizes = THE INFO-BOX LADDER (18/16/14 — one ladder across both components; the SG/Inter body split dies). **EXCLUSION LAW** (double-line lesson): divided grids strip `--rule-top` mechanically. **FENCES**: rule-LEFT is not a blurb axis (blockquote-family territory — info-bar stays separate); no alignment axis (AIF's centered old cert dies). RETIRES: both cert-cards, nl-benefits skin, dark-blurb, footer-blurb, lp-what cells. `.reveal` stays a tracked behavior candidate. `.hero-card` box adoption = later scope-map step. |
| 2026-07-06 | INFO BAR — the first benefits-family member SHIPPED (operator V2 verdict, sandbox-judged) | Judged live on the sandbox `infobar` intensities + the `aif-home-v2` homepage clone. **SEPARATE COMPONENT `.info-bar`** (production's AIF name; AIG `homepage-infobar` aliases). THE V2 SKIN: 4px `--brand` left rule (was 2px) + **BOLD** statements (were regular) + **LEFT PADDING = THE PEREX INDENT** (operator: same as blockquote — `calc(--flow-indent − --stroke-4)`, text lands at 24). Band fill = `--raised` (surface-riding) — this RESOLVES AIG's live light-flip experiment: the bar has no opinion, the host section picks the world. **1..n statements** (operator: /newsletter carries 4): border-box 220px wrap floor, 4 fit one container-max row; ≤767 stacks. 0 new tokens. Family rest still awaits the A/B/C verdict. |
| 2026-07-06 | Sticky bar — REFACTORED to definition: THE BAR RUNG (control-scale scope) | Operator: the section sat between inventory and definition. Restructured to THREE ORTHOGONAL FACTS — SHELL (surface-riding fixed-bottom slide shell) · **THE BAR RUNG** (the bar is a CONTROL-SCALE SCOPE per the scale=scope precedent: `.sticky-bar .btn` + wrappers read `--bar-control-height` 40px / `--bar-control-size` = caption-size — ONE voice for field, submit, mobile button AND CTA; markup carries NO ladder rung on bar buttons; paired buttons stretch with the field and relax together on touch) · TYPE = COMPOSITION + the chatbot axis. Strays unified into the rung (GM): AIF 16px mobile default, btn--sm 38px CTA height. Gate asserts one 14px voice + 40px rung across all bar controls. |
| 2026-07-06 | Sticky bar — SHIPPED; THE SAMPLER is the general surface mechanism; 1 new token | STICKY-BAR-MAP §5 resolved (operator): **ONE `.sticky-bar` primitive** (production's own extracted name) × TYPE compositions (`--email` capture bar · `--button` CTA bar · the save bar composes freely) × `--chatbot-clear` axis (100px calibrated) × SURFACE. **THE SAMPLER GENERALIZED** (the AIG course behavior the operator wanted everywhere): `[data-sticky-sample]` reads the rendered background behind the bar (elementsFromPoint → first opaque ancestor → luminance) and toggles **`.section-dark` on the bar** — roles re-skin everything; sections need NO registration/component status, only opaque backgrounds; re-samples only while visible (no slide-out flash); failure → light. Engine = the AIG data-anchor canon + `data-show-gate="top"` (course hero) + `data-show/hide-fraction` (the landing's page geometry — its inline DB script dies) + `data-suppress-key`. Skin canon = slim light article bar (40px controls via the SMALL FIELD SCALE composed into `__form` + the input-pair stretch; quiet 1px edge — both operator-ruled in production comments; the landing's tall dark skin + 2px accent stripe die). **NEW TOKEN `--shadow-up`** (approved 2026-07-06) — the upward shadow, visible-only (phantom-glow fix kept). **CHATBOT AXIS IS ALSO A MOBILE-SHAPE AXIS** (correction, same day — the first cut wrongly reset the clearance on mobile): no chatbot → full-width mobile button (AIF harvested); `--chatbot-clear` → 100px clearance survives every viewport + natural-width 14px button, left-aligned (AIG harvested: "NOT full width — the chatbot bubble lives bottom-right"). GM: mobile cut 900→1023, meta cut 1279→1023, 1280→--container-max, slides→--transition-smooth, z 90→40, consent armor→global chain, 901–1150 patch→base nowrap. Adoption: `.lp-sticky`/`.sticky-cta` alias; the /newsletter `landing_html` DB edit = operator action. |
| 2026-07-08 | Sticky bar — THE PITCH REFRAME: benefit leads, consent demotes (operator: "add the user BENEFIT above the consent line", sandbox Option A) | The `--email` pitch becomes a VERTICAL STACK (`flex-direction: column`, `--spacing-4` gap): NEW element **`.sticky-bar__benefit`** LEADS — the user reason to subscribe, `--body-md-size` **bold** in `--text` (`--leading-heading`) — and **`.sticky-bar__consent` DEMOTES** from `--caption-size` (14) → **`--meta-size` (12)**, quiet beneath the benefit. GDPR-safe: the consent copy + privacy link stay VERBATIM — only placement + size change; **12px is the floor, never lower**. DISTINCT from the form `.consent-note` / `.selection-*` consent component (that stays caption-size — different element, [consent.md](components/consent.md)). Benefit slot is OPTIONAL (a consent-only pitch still renders; single child = stack no-op). Specimen + gate grown (pitch flex-direction column + benefit 16/700 + consent 12). Theme wiring: the AIF article bar pitch becomes admin/Polylang-editable (`article_sticky_pitch` string pair, `pll__()`); landing bar pitch = the `landing_html` DB edit (operator action). 0 new tokens. |
| 2026-07-08 | Surface tokens — light-2 `--bg-alt` COLLISION fixed (operator: "re-declare --bg-alt in the scopes; factory + gate — yes, do that this way") | **The real collision is light-2 ONLY.** light-2's `--bg` is `{gray-50}` (#f7f8fb AIF / #f2f2f2 AIG) and it INHERITED `--bg-alt` = `{gray-50}` from the base semantic → identical → every rail/band fill (pagination pill, zebra band) MELTED into the section. Fix: re-declare `--bg-alt` = **`{gray-100}`** in `tokens/scopes/light-2.json` (SOURCE — `tokens.css` is generated). Visible on both brands (AIF #e4e7ec Δ19 vs bg / Δ10 vs raised; AIG #e5e5e5 Δ13 vs bg). **light-3 LEFT AS-IS**: its `--bg` is `{gray-150}`, so the inherited `--bg-alt` = gray-50 was ALREADY distinct (AIF #f7f8fb Δ9, AIG #f2f2f2 Δ14 — a visible, a11y-clean lighter rail); it was never a real `--bg-alt` collision (what "vanishes" there is the RECORD CARD, which uses `--bg` + `--border` — a SEPARATE role question, not this fix). **`{gray-200}` REJECTED**: it separates more on AIG, but the darker swatch broke the a11y gate (the Colors reference tab's gray-200 token swatch label fell below contrast on aifounders/colors) — a11y is never weakened. `{gray-100}` on light-3 was also rejected — AIG's ramp is TIGHT and non-monotonic (gray-150 #e4e4e4 ≈ gray-100 #e5e5e5, Δ1 invisible; and AIF gray-150 #eef2f6 is *lighter* than gray-100 #e4e7ec). Base light surface (light-1) untouched. Gate grown (`tokens.spec.js`: `--bg-alt` ≠ `--bg` AND ≠ `--raised` on light-1/2/3, both brands) + a11y baseline stays clean. 0 new palette values. |
| 2026-07-05 | Selection hover = THE INPUT BORDER TRANSFORM (operator: the --link jump was too much) | Checkbox/radio chips now carry the input wrapper's exact pair: **rest `--field-border` → hover `--field-border-strong`** (was rest strong → hover `--link`) — ONE hover idiom across every input component. Scope-safe: brand maps both roles to black (the job-board chip keeps its black rule), dark steps 700→600. Polish: disabled chips no longer hover (`:not(:has(:disabled))` guard — the old rule repainted disabled borders). Also: checkbox + radio GROUP labels demo the `.form-mandatory` marker (it composed for free via `.form-label-row`; now shown + gate-asserted on both group specimens). SUPERSEDES the forms-batch "hover uses --link" note. Gate: new SELECTION HOVER test (rest/hover/disabled/never-link + group markers); the FORMS-model dark-chip assertion updated to the new rest role. |
| 2026-07-05 | Modal polish + MANDATORY MARKER de-redded (operator round 3) | (1) **Box width was 96px off**: production's container is CONTENT-BOX — 560px content + 48px padding = **656px rendered** (live-measured on aiguild.cz); the DS border-box 560 squeezed the form. Fixed: `max-width: 656px` border-box + the outer `.modal` inset removed (production has none). (2) **Scroll engine verified + polished**: 90vh + `overflow-y: auto` matches production; `overscroll-behavior: contain` now on the container at ALL sizes (production: mobile only). (3) **Composed content**: `.modal__text` intro voice + any DS `.info-box` composes into the modal flow — specimen + gate cover both. (4) **`.form-mandatory` — brand deep, NOT red** (operator: breaking the theme unnecessarily = violation; the harvest agrees — BOTH FF overrides comment "Required asterisk — brand deep instead of red", AIF `#0067b1` = `--deep` verbatim; AIG's stale `#0D1B48` copy-drift dies, the commented intent wins). `--status-error` reserved for actual errors; Tier-1 `--deep` read flagged like the accordion arrow. |
| 2026-07-05 | FORM STACK — the missing composition primitive (operator: "clean form, design-system based") | The modal's broken form demo (bare unsized submit, hand-rolled rhythm) exposed a REAL DS GAP: the field system had elements but NO NAMED STACK — every specimen hand-rolled `flex column gap:16px` inline, `.aif-form` (a THEME class) leaked into DS demos, and nothing said a submit needs a ladder rung. **Minted `.form-stack`**: flex column, `--spacing-16` group rhythm (production FF `12→20px` margins + the forms-batch demos' 16 NORMALIZED to one gap, GM), `> .btn { align-self: flex-start }` (no stretched buttons). LAWS: every form composes `.form-stack` › `.form-group` (+`.form-label-row`) › `.form-control-wrapper`; **submit buttons REQUIRE a ladder rung** (base `.btn` has no dimensions) — modal = `--md`, comment form = `--sm` (harvested). Fixed: modal specimen (label rows + mandatory marks + btn--md), comments edit+comment forms (`.aif-form` → `.form-stack`), the forms specimens now DOGFOOD the class. Gate asserts the stack contract on the modal (gap 16, wrapped fields, labeled rows, 52px submit, flex-start). |
| 2026-07-05 | Modal — CORRECTED, rebuilt from scratch (operator veto) | The first cut invented a close-less `--narrow` "consent dialog" variant from the AIF author-portal one-off — **operator: "doesn't exist", fully hallucinated as a component. WITHDRAWN.** THE modal = the FORM MODAL (AI Guild's Fluent-Forms modals): title + form body + ALWAYS a close button. `--narrow`/`__desc`/`__actions` deleted; the specimen rebuilt to compose the DS FORM SYSTEM (form-control-wrapper fields, consent row, primary submit — the shape Fluent Forms maps onto). The AIF newsletter-modal + nl-modal consent popup reclassified: page one-offs, NOT ported. |
| 2026-07-05 | Modal — FIVE production copies collapsed into one canon | Census: AIF `.registration-modal` (the only copy with the full a11y contract), AIG `.reservation-modal` (near-twin CSS + the STRONGER mobile dvh+overscroll fix), AIG inline `.modal` contact modal (markup+script copy-pasted in 3 templates, naive display toggle), AIF `.newsletter-modal` (jQuery, blur backdrop, detach-to-body transform-trap hack), AIF `.nl-modal` consent (inline `<style>`). ONE `.modal` canon: engine = registration's (ported+generalized, `js/components/modal.js`, `data-modal-open` triggers, `window.aigdsModal` API); mobile = reservation's dvh sheet (⚠️ veto points, all stronger-twin/snap precedents): scrim = the two dedicated modals' rgba(7,7,8,.7) majority → `color-mix(in srgb, var(--black) 70%, transparent)` per the no-alpha-palette overlay ruling — the .5/.6/.8+blur strays die; title 32/700 (+ AIG Space-Grotesk hardcode drift) → the **heading-md bundle**; fade 0.3s → `--transition-normal`; newsletter `--shadow-xl` dropped; 640 → 599 snap. Sizes 560 (form canon) + `--narrow` 480 (dialog canon), 40px close hit, 64px sheet top clearance, z-index 9999 (unanimous) = calibrated constants. Canon markup mounts at body level — the transform-trap hack dies. Theme wiring (hidden fields, 4/5/7 form switching, consent AJAX) stays theme JS on the DS API. 0 new tokens. |
| 2026-07-05 | Engagement + Comments — SPLIT into two components (operator); both distilled | Operator ruling: AIG also has engagement but NO comments — the queue's single "social engagement" row splits. **ENGAGEMENT** (Aha! + share + toast): AIF components.css:5556-5842 and the AIG copies are BYTE-IDENTICAL (CSS, markup, engagement.js) — verbatim cross-brand twins, 0 divergences; production `aif-` class names KEPT (already the cross-brand namespace). THE ADDTOANY BOUNDARY: DS owns the widget + toast + clone-and-rewrite engine (js/components/engagement.js, ported verbatim, degrades gracefully without data-ajax-url); the counting AJAX + the plugin-output-hiding `.single` rules stay theme/plugin. GM: toast shadow → `--shadow-sm`, slide 250ms → `--transition-normal`, 600 → 599. **COMMENTS** (AIF-only, full anatomy): Medium-style thread (no bubbles), avatar-derived L-connector geometry (48→y24 / 40→y20, 72/56 indents) ported verbatim as calibrated constants; avatars now COMPOSE the DS avatar via NEW `--xs` (48px) + `--initials` (`--brand`+`--text-on-brand`, numbered-headings precedent) modes; author/avatar links collapse onto `card-title-link`/`card-image-link`; action links+buttons ALIGNED TO THE LINK IDIOM (hover-removes-underline outlawed, the blue !important armor → `--link` roles); tombstone avatar #ebeef3 → `--raised`; edit-cancel 600 → `--weight-bold`; the !important LIST ARMOR against article prose counters KEPT. Edit/delete engine (15-min window, GDPR tombstone, comment-edit.js) stays plugin territory; the guest register-banner stays a separate candidate. 0 new tokens (both). |
| 2026-07-05 | Course card — distilled; 3 NEW tokens (operator-approved); SLOT-DRIVEN ORIENTATION | Living component = AIG's `--editorial` card ONLY (5 contexts; active/inactive states; per-course eyebrow accents). DEAD not ported: base 56px-icon card, `--vertical/--inverted/--no-illustration/--product` (zero markup), BOTH AIF CSS blocks (the `[add]` shortcode emits `.aif-native-promo` since 2026-06 — the yellow !important CTA + ad-inline centering die too). **ORIENTATION = THE SLOT** (persona precedent): `.course-card-slot` container, ≥720px → horizontal (fixed 420px image track, operator-ruled; centered content), below → vertical (16/9 top image, top content, pinned CTA) — production's viewport MQ AND the kurz `count-3/4` overrides collapse into the one rule; both constants live-measured on aiguild.cz. **NEW TOKENS (operator 2026-07-05)**: `--leading-snug` 1.05 + `--tracking-display` -0.022em (letter-spacing is now a TYPOGRAPHY DIMENSION: label · display) for the display title treatment on the heading-lg voice; `--bg-base` = pop-to-page-surface role (white on every light scope, black on dark + brand — the kurz white-fill override becomes canon; ⚠️ veto point: brand scope maps to black, following --raised's judgment). GM: eyebrow 0.1em → `--tracking-label`; CTA = `.btn--md` rung verbatim (52px; pad 24→16, gap 12→8); `::selection` hardcode + self margin-bottom + dead `__body` max-width DROPPED. Eyebrow accents = Tier-1 palette reads (brand-strong / secondary / magenta / lime) — production's on-white #c9a101 special case IS `--brand-strong`. |
| 2026-07-05 | Preview card — SINGLE SEPARATOR LAW (double-line bug) | Operator caught a double line between stacked rows when the row below is no-photo (its top hairline met the row above's bottom hairline). HARVESTED fix = production's signal-archive mechanism (`.articles-grid--signals .article:nth-last-child(n+5) { border-bottom: none }` per breakpoint): **the CONSUMER grid strips `border-bottom` from every non-last-row card** (`:nth-last-child(n+cols+1)`) — the no-photo row's own top hairline becomes the ONLY separator, photo rows meet the row above with no line (production homepage behavior), the final row closes the stack. Stays consumer CSS (selector math needs the consumer's column count per breakpoint); the specimen 3×3 implements it as the reference consumer; gate asserts stripped bottoms rows 1–2, solid last row, solid no-photo tops, no photo-row top lines. |
| 2026-07-05 | Preview card — refinement round (meta ∞ · destructive-last · stacking contract) | (1) `__meta` = **1..n facts, unbounded** ("A / B — can be also C / D — basically infinite"). (2) Actions grammar = **Action 1..n with DESTRUCTIVE ALWAYS LAST** — enforced mechanically (`.preview-card__actions .btn--destructive { order: 99 }`), gate-asserted with a deliberately out-of-order specimen. (3) **STACKING CONTRACT**: grid stretch + flex column + `margin-top:auto` on actions ⇒ per grid row, ALL bottom hairlines and ALL action rows align regardless of content length — simulated in the specimen's 3×3 grid (`#preview-stack-demo`, 9 deliberately unequal cards) and pinned in the gate (unique bottoms = 1, unique action-tops = 1 per row). Consumers must not override `align-items` on preview-card grids. |
| 2026-07-05 | Reference card — QUOTE VOICE finalized on AIG's fixed grammar; abstraction WITHDRAWN | Operator: the first voice cut was flawed (bullets off, ol counters off-color, headlines over-sized) — "most of it has been already fixed in the AIG theme". FINAL grammar = the AIG case-study fix: li text = `--text-secondary` (same as p); BOTH markers (ul arrows + ol counters) = `--text-disabled`; counters INHERIT the text's font/size/weight (never bold); harvested 40px indent + `margin-top:7` arrow alignment; **headlines h1–h6 = strong as a block** (SAME size as text, bold — not lead-size). The generic `.quote-voice` hook is WITHDRAWN ("forget about the Blue protocol… this level of abstraction is simply not possible now") — the protocol card distills standalone later. Also same-day: quote mark → top-right avatar MIRROR (64px, Lazzer, --brand-tint); card ruled BORDERLESS. |
| 2026-07-05 | Reference card — quote mark, QUOTE VOICE abstraction, light personal, responsive | (1) Quote mark = **Lazzer `“` glyph in `--brand-tint`** (very muted brand, alpha-robust on both surfaces) — the harvested SVG brackets replaced (icon stays archived in the catalog). (2) **THE QUOTE VOICE** abstracted: `.reference-card__content` + generic `.quote-voice` hook — p/li/strong ONE font (Space Grotesk) ONE size; **h1–h6 = ONE identical headline style** (lead-size bold accent, deliberately no ramp); lists full-size (the broken mixed-size lists are dead); protocol card (o-nas) inherits `.quote-voice` at its distill. (3) Classic personal testimonial (light, avatar+quote) added to the canon — it is the bare canonical card, no scope class. (4) Responsive ported from AIG ≤1023 (stack, quote 60px) without the !important armor. |
| 2026-07-05 | Persona card — watermark meta + linked version | (1) The location meta = **WATERMARK voice**: `--text-disabled` dimmed further to opacity .5 + quiet tracking, every orientation ("too dignified" before). (2) **Two versions**: unlinked (default) and LINKED when the persona has a detail page — photo wraps in `a.card-image-link`, name in `a.card-title-link` (existing chain-excluded utilities; inherit color, no rest underline, decent hover). Pure composition, no new CSS classes. |
| 2026-07-04 | Persona card — final polish rulings | (1) **Portrait FULL HEIGHT — ALWAYS** in horizontal mode: absolute-fill img (zero intrinsic contribution — content sizes the row, the crop zooms), track `clamp(200px, 40cqi, 320px)`, min-height guard; the top-anchored-square + whitespace default REJECTED and deleted. (2) **AIG-clean header UNIFIED**: no divider between position and bio (AIF's border + padding were the space-wasters). (3) **Socials hover = DECENT**: opacity .5→.8 only, no color jump — not a primary action. (4) Bottom block: location pins to card bottom, socials below it, lowest. |
| 2026-07-04 | Sweep review (both-theme component sweep) | **Position filters** (`.position-filter__pill`, AIG positions board) = plain checkboxes in a costume — NOT a component; refactor onto the DS checkbox at adoption. **Position preview** = a VARIANT of the article/signal/event preview card (adds a skills row), not a separate component — folded into the queued preview-card row. **Breadcrumb + Pagination = REAL** — promoted from sweep candidates to the distillation queue. Course preview card confirmed = `.course-info-card` (naming only). |
