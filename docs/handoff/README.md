# Handoff — pointer

**The live handoff brief is [`docs/HANDOFF.md`](../HANDOFF.md).** Start there —
it names the operating manuals (the `ds-colors` / `ds-typography` skills), the
environment (WP container on `localhost:8090`), THE RITUAL, and the current
work state.

Also in this directory:

- [`WORDPRESS_IMPLEMENTATION.md`](WORDPRESS_IMPLEMENTATION.md) — how the
  plugin integrates with WordPress (enqueues, copy manifest, shortcodes).
- [`WORDPRESS_FRICTION_ANALYSIS.md`](WORDPRESS_FRICTION_ANALYSIS.md) — known
  WP friction points.

Historical note: this file previously held the v1-era status (2026-01-25:
static HTML templates, VRT golden masters, the v1 component list —
navigation, reference/persona cards, accordion, footer, etc.). That
generation is archived in `dev/v1-archive/`; the v2 component ledger is
[`docs/IMPLEMENTATION_STATUS.md`](../IMPLEMENTATION_STATUS.md) and the
per-component contracts live in [`docs/components/`](../components/).
