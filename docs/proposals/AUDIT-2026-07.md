# AUDIT 2026-07 — the full VECTOR-DS system audit (roadmap item 1)

**Status: COMPLETE 2026-07-06.** Executed per the agreed plan: Phase 0
(L2 instrument) → A (inventory) → B (tokens) → C (architecture rulings) →
D (L3/L4 + this report). Every fix shipped through the ritual; the gate
grew from 82 to **92 checks** during the audit itself.

## The headline

**The system is sound.** Four inventories align perfectly, the token
graph is parity-clean with zero undefined references, docs carry zero
real drift, and the retrieval-row architecture validated 40/40 on its
first run. The audit's real catches were *behavioral*: four broken-screen
bugs the old gate could not see (the operator's exact prediction) and one
a11y contract gap (modal focus restore) — all fixed.

## What was built (the gate, L0→L4 complete)

| layer | file | state |
|---|---|---|
| L0 | `build/lint-css.mjs` + CI tokens-sync | existed |
| L1 | `tests/tokens.spec.js` — 82 contracts | existed |
| **L2** | `tests/layout.spec.js` — auto-discovering broken-screen sweep (overflow · wide · occlusion via `elementFromPoint` · zero-size · console/pageerrors · dead requests), 2 brands × 2 viewports | **NEW** |
| **L3** | `tests/a11y.spec.js` — axe (wcag2a/aa), demo-region-scoped, **no-NEW-violations vs the committed `tests/a11y-baseline.json`** (180 entries / 31 tab-slices; ratchet = Phase B burn-down) | **NEW** |
| **L4** | `tests/interactions.spec.js` — behavior contracts, pilot: modal + accordion (doc prose = assertions, §10 law 3); header/sticky-bar behaviors already live in L1 | **NEW** |
| shots | `build/shots.mjs` (`npm run shots`) — operator-eyes screenshots, never asserted | **NEW** |

## Findings → fixes (all shipped)

1. **Newsletter capture never stacked on mobile** (canon): the hero form
   lost production's <600 column base; the footer-dark variant lost its
   ≤767 stack entirely. Both restored. *(L2 catch — real broken screens.)*
2. **Styleguide chrome broke mobile**: 11 unwrapped `.sg-table`s (one
   1333px wide) + long code paths in headings. Chrome-level fix
   (self-scrolling tables, `overflow-wrap` on headings).
3. **Preview-card 3×3 simulation** overflowed at 375 — now scrolls in its
   own wrapper, geometry preserved.
4. **Modal focus restore missing** (engine + doc): closing now hands
   focus back to the opener; the doc carries the promise; L4 asserts it.
   *(Found by authoring the contract — the audit working as designed.)*
5. Documentation completeness: AddToAny vendor selectors + the comments
   runtime state documented as theming contracts; `button-group`
   alignment variants documented as reserved; VECTOR-DS §6 dropzone class
   name corrected (`.is-dragover`).

## Verified clean

- **Inventory**: 31 CSS sections ↔ 40 ledger rows ↔ 39 docs (+README;
  links.md serves 2 rows) ↔ 43 specimens; every specimen renders; 385
  canon classes, zero true orphans.
- **Tokens**: 298 emitted vars; palette parity 64/64 PERFECT; zero
  undefined references (17 component knobs correctly component-local);
  46 reserved-API tokens reconfirmed, zero retirements.
- **Docs**: zero real drift (all flags were intentional theme-class
  references or explicit negatives).

## Architecture rulings (Phase C)

- **`.ds.yaml` is DEAD — rows are GENERATED.** `build/rows.mjs` derives
  the VECTOR-DS §7 rows from the ledger + component docs → 
  `assets/ds-rows.json` (40 rows, zero gaps, `git_ref`-stamped for
  changed-row ingest). One source, no drift.
- **Superseded-by-shipped, recommend RETIRE** (operator ratification):
  the AGENTIC-DS-RESEARCH per-component folders + `button.yaml`, the
  `registry.php` REST registry, and the WordPressbook/Storybook-server
  leg — the ledger + docs + generated rows + specimen system now serve
  every consumer those were designed for. Tier-3 component tokens:
  **ratify the component-knob pattern** (`--header-*`, `--bar-control-*`,
  `--footer-*`, `--stack-*`…) as the tier-3 answer — knobs on the
  component scope, values always tokens.
- **SCOPE-CENSUS: resolved** (superseded by the footer/header verdicts —
  dark-2 lives, `.surface-support` became authorable); light-3 watchlist
  stands.

## Open operator verdicts (the audit's out-tray)

1. **HEADING-RAMP** — unify page headings down to the article ramp
   (page h2 36–44 → 28)? Real sitewide design change; proposal stands.
2. **Sweep candidates** — 2 resolved, 2 partial (toast promotion, page
   logo-grid), 10 open (ad card, protocol card, nl-preference card,
   archive header, register banner — comments guest-state consumer
   waiting, video embed, podcast player, hero family — its tokens are
   already reserved, application row). Pick the next distills.
3. **Phase-C retirements** above (folders/registry/WordPressbook) —
   ratify or keep.
4. **A11y ratchet plan** — 180 baseline entries (142 color-contrast, 22
   link-name, 6 aria-required-children, 4 scrollable-region-focusable, 4
   label, 2 aria-hidden-focus). Burn-down order proposal: structural
   rules first (aria/label/link-name ≈ 34, mostly specimen fixes), then
   the contrast review (real design calls — tertiary/quaternary voices on
   colored surfaces).
5. **PUBLIC-REPO-PLAN §6** — plugin rename, LICENSE holder, proposals
   list, version (unchanged, needed before the public flip).

## Hand-off

The audit unblocks: the **cleanup pass + final sync to
`aifounders-designsystem`** (roadmap 3) and the **vectorization pipeline**
(roadmap 4 — the rows exist; remaining: Supabase table + RPC + n8n ingest
+ the ds-lookup skill + the MCP tool). Figma import (roadmap 2) can run
any time and backfills `figma_node_id`/`code_connect` into the rows.
